name: Hello World

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    # 실제 동작하기 위한 코드
    steps:
    #- uses: actions/checkout@v2
    - name: Run pwd
      run: pwd
    - name: Run ls -al
      run: ls -al
      
