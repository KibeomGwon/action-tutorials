name: github checkout

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2 # 다른 사람이 작성한 스텝들을 사용하고 싶을때 사용
    - name: Run pwd
      run: pwd
    - name: Run ls -al
      run: ls -al
    
